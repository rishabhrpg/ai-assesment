# Seed data

Seed data is applied by the Bun script:

- [`../src/server/src/scripts/seed.ts`](../src/server/src/scripts/seed.ts)

Run:

```bash
bun run db:seed
```

This folder exists to satisfy the assessment repository layout (`database/seed-data/`). Optional: add `.sql` snapshots here in the future for alternative seeds.
