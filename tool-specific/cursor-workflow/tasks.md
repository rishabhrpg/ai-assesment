# Tasks (Cursor)

- [x] Scaffold Bun workspaces (`src/server`, `src/client`) + root scripts + `.env.example`
- [x] Add SQLite migration + Drizzle schema + migrate/seed scripts
- [x] Implement Hono routes + Zod validation + CORS + status machine
- [x] Build React pages (list, create, detail) + Tailwind states
- [x] Add integration tests for transitions (`tests/`)
- [x] Write lifecycle markdown + `ai-prompts/` + this Cursor workflow folder
