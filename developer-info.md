# Developer info

- **Project:** Support Ticket Management System (AI assessment build)
- **Primary stack:** Bun, Hono, React, TypeScript, Tailwind, SQLite, Drizzle
- **Primary AI tool:** Cursor (see `tool-workflow.md` and `tool-specific/cursor-workflow/`)
- **Repository layout:** Matches the assessment guide (root-level lifecycle docs, `ai-prompts/`, `database/`, `src/`, `tests/`).

## Local setup summary

1. `bun install`
2. `bun run db:migrate` && `bun run db:seed`
3. `bun run dev` — API `:3000`, Vite `:5173`

## Dates

Fill in for your submission:

- **Start date:** _YYYY-MM-DD_
- **Submission date:** _YYYY-MM-DD_
